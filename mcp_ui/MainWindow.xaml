<Window x:Class="MCP_UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MCP AI Assistant" Height="450" Width="800">
    <Grid>
        <TabControl>
            <TabItem Header="AI Prompt">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <Label Content="Enter your prompt:" Grid.Row="0"/>
                    <TextBox x:Name="PromptTextBox" Grid.Row="1" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10"/>

                    <Button Content="Browse for Reference Files..." Grid.Row="2" Margin="0,0,0,10"/>
                    <Button Content="Generate and Run" Grid.Row="3"/>
                </Grid>
            </TabItem>
            <TabItem Header="Settings">
                <Grid Margin="10">
                     <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Label Content="OpenRouter API Key:" Grid.Row="0"/>
                    <TextBox x:Name="ApiKeyTextBox" Grid.Row="1" Margin="0,0,0,10"/>
                    <Button Content="Save Key" Grid.Row="2" VerticalAlignment="Top" HorizontalAlignment="Left" Width="100"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window> 